# Model Evaluation Report — {{ date }}

> Auto-generated by `LedgerFlow.evaluation.report`. Do not edit by hand —
> re-run `dvc repro` to refresh.

## Summary

| Model | AUC-ROC | Avg Precision | Brier Score | Log Loss | Precision @ 5% FPR | F1 | Recommended |
|---|---|---|---|---|---|---|---|
{% for name, m in models.items() -%}
| {{ name }} | {{ "%.4f"|format(m.auc_roc) }} | {{ "%.4f"|format(m.avg_precision) }} | {{ "%.4f"|format(m.brier_score) }} | {{ "%.4f"|format(m.log_loss) }} | {{ "%.4f"|format(m.precision_at_5pct_fpr) }} | {{ "%.4f"|format(m.f1) }} | {{ "✅" if name == recommended else "—" }} |
{% endfor %}

## Recommendation

**{{ recommended }}** is recommended for the production A/B test.
{{ recommendation_reason }}

Calibration matters here because the model emits probabilities: lower Brier score
and log loss mean the predicted probabilities are closer to observed frequencies.

## Top {{ top_features|length }} Features (by mean importance across models)

| Rank | Feature | Mean Importance | {{ recommended }} Importance |
|---|---|---|---|
{% for f in top_features -%}
| {{ loop.index }} | `{{ f.feature }}` | {{ "%.4f"|format(f.mean_importance) }} | {{ "%.4f"|format(f[recommended]) }} |
{% endfor %}

## Low-Signal Features (candidates for removal)

{% if low_signal_features -%}
{{ low_signal_features|length }} feature(s) scored below the importance threshold
of {{ low_signal_threshold }} across all models and are candidates for removal in
the next iteration:

{% for f in low_signal_features -%}
- `{{ f }}`
{% endfor %}
{%- else -%}
No features fell below the importance threshold of {{ low_signal_threshold }}.
{%- endif %}
